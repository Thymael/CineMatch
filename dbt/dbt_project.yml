name: "cinematch"
version: "1.0.0"
config-version: 2

profile: "cinematch"

model-paths: ["models"]
test-paths: ["tests"]

target-path: "target"

clean-targets:
  - "target"
  - "dbt_packages"

models:
  cinematch:
    staging:
      +materialized: view

    marts:
      +materialized: table